---
label: Frontend Post
hide_body: false
fields:
- type: text
  name: title
  label: title
- type: datetime
  name: date
  label: date
  default: now
  config:
    required: true
- name: categories
  type: select
  default: frontend
  config:
    required: true
    options:
    - frontend
    - life
    - others
    source:
      type: simple
      section: frontend
      file: 
      path: 
  label: categories
- name: tags
  type: tag_list
  default:
  - frontend
  label: tags
- type: text
  name: excerpt
  label: excerpt
  config:
    max: 200
